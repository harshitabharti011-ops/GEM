/target
/Cargo.lock
/compile_commands.json
.cache
.DS_Store
build/

# Synthesis and simulation outputs. Regenerated by synth/run_synth.sh,
# bench/make_vcd.py and bench/verify_ladder.sh, so they are build products
# rather than sources -- and they are large (a .gemparts is ~1 MB, a VCD
# several hundred KB). The scripts that produce them ARE tracked, which is
# what the submission needs.
build_native/
build_shred/

# Scratch logs used to shuttle command output during development.
.out.txt
*.log
